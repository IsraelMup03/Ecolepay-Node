# Utilise en production par `npm run build` (backend et frontend sont servis par le
# meme processus/port une fois construits, un chemin relatif fonctionne donc partout
# sans avoir a connaitre a l'avance le port ou l'hote de la machine du client).
VITE_API_URL=/api
